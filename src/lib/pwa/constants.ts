/** Cookie posé en mode standalone (ajout à l’écran d’accueil) pour éviter la landing. */
export const PWA_STANDALONE_COOKIE = "soline-pwa-standalone";

/** ~400 jours — relancé à chaque ouverture en mode appli. */
export const PWA_STANDALONE_COOKIE_MAX_AGE_SEC = 60 * 60 * 24 * 400;
